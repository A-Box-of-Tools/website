/* Built from https://github.com/A-Box-of-Tools/website by build.py. Verify with: python build.py --check (names mangled by esbuild) */
function o(e){return e<1024?`${e} bytes`:e<1024*1024?`${(e/1024).toFixed(e<10240?1:0)} KB`:`${(e/(1024*1024)).toFixed(2)} MB`}function h(e){return e.toLocaleString("en-US")}function l(e,n){if(!e)return"";const t=Math.round((n-e)/e*100);return t===0?"the same size as the file":t>0?`${t}% larger than the file`:`${-t}% smaller than the file`}function d(e){return e<=2*1024?{level:"good",text:"Small enough that this is a clear win: one fewer request, and nothing much added to the file it lands in."}:e<=10*1024?{level:"good",text:"A normal size for an inlined icon. Worth it for something that appears on every page."}:e<=50*1024?{level:"fair",text:"Large for an inline picture. Everything that includes this stylesheet now carries it, and it cannot be cached on its own."}:{level:"poor",text:"Too big to inline. Served as an ordinary file this would be cached once and fetched in parallel; inlined, it is on the critical path of every page and re-downloaded whenever anything around it changes."}}function c(e,n){const t=n?Math.round(e.bytes/n*100):0,r=a(e.kinds),i=t>=5?`, which is ${t}% of the file`:"";return`Carries ${o(e.bytes)} of ${r}${i}. It is copied into the URI along with the picture.`}function a(e){return e.length<=1?e[0]??"":`${e.slice(0,-1).join(", ")} and ${e[e.length-1]}`}function s(e,n){return`${e} \xD7 ${n}`}export{o as bytes,h as count,s as dimensions,a as list,c as metadataNote,l as overhead,d as verdict};
