/* Built from https://github.com/A-Box-of-Tools/website by build.py. Verify with: python build.py --check (names mangled by esbuild) */
function s(e){return e<1024?`${e} bytes`:e<1024*1024?`${(e/1024).toFixed(e<10240?1:0)} KB`:`${(e/(1024*1024)).toFixed(2)} MB`}const c=(e,t)=>`${e} \xD7 ${t}`,d=e=>`${e} image${e===1?"":"s"}`;function p(e,t,r){return t==="ico"&&r?"favicon.ico":`${o(e)}.${t}`}function o(e){return e.replace(/\.[^.]+$/,"").trim()||"icon"}const u=e=>o(e).replace(/[\\/:*?"<>|]+/g,"-");function $(e,t,r,n){if(!e.length)return"Tick at least one size.";const a={auto:"stored the pre-Vista way up to 64 pixels and as PNG above that",png:"every size stored as PNG",bmp:"every size stored the pre-Vista way"},i={pad:n?"padded to a square, with the padding left transparent":"padded to a square on the background colour",crop:"cropped to the square in the middle",stretch:"stretched to a square"};return`${e.length} size${e.length===1?"":"s"} - ${e.join(", ")} - ${a[t]}. A picture that is not already square is ${i[r]}.`}export{s as bytes,d as countOf,$ as describe,c as dimensions,u as folderFor,p as iconName,o as stemOf};
