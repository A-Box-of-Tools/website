#
# Guide: making a QR code that actually scans.
#
# Written for the person who has made a QR code, printed it, and watched it
# fail on somebody's phone - or who is about to. What the error-correction
# level is for, why the white margin is part of the code, what "dynamic" means
# and what it costs, and why a code on a poster is a different job from a code
# on a screen.
#
# Prose lives next door in body.html.
#

slug = "guides/make-a-qr-code"
kind = "guide"
nav = "Making a QR code"
group = "doing-the-job"
tool = "qr-barcode"

title = "How to Make a QR Code That Scans Every Time"
description = "Which error-correction level to pick, why the white margin around a QR code is part of the code, how big to print it, and what a free generator's 'dynamic' code costs you later."

heading = "How to make a QR code that still scans on somebody else's phone"
lede = """
    Making a QR code takes a second. Making one that works on a wet menu, a bus
    shelter, or a phone held at arm's length in bad light takes four decisions,
    and all four are made before you print anything. Here is what each one
    does."""

updated = "20 August 2026"
published = "2026-08-20"
lastmod = "2026-08-20"

og_title = "Making a QR code that still scans on somebody else's phone"
og_description = "The four decisions that decide whether a printed QR code works: the level, the margin, the size, and whose address is actually inside it."
og_image_alt = "abox.tools - tools that never touch a server"
