#
# Guide: converting an SVG to a PNG.
#
# Written for the person who has been asked for "a PNG of the logo" and has to
# pick a number of pixels out of the air: where that number actually comes
# from for a screen, an app icon and a printer, and which three things change
# the moment a drawing stops being instructions and becomes pixels.
#
# Prose lives next door in body.html.
#

slug = "guides/convert-an-svg-to-png"
kind = "guide"
nav = "Converting an SVG to PNG"
group = "doing-the-job"
tool = "svg-to-image"

title = "How to Convert an SVG to a PNG at the Right Size"
description = "A vector has no pixel size of its own, so the number is yours to choose. Where that number comes from for a screen, an app icon and a printer, and what changes when a drawing becomes pixels."

heading = "How to convert an SVG to a PNG at the right size"
lede = """
    Converting is the easy half. The question that decides whether the result is
    any use is the one nobody hands you an answer to: how many pixels? This is
    where that number comes from, and what a drawing loses on the way to
    becoming one."""

updated = "26 August 2026"
published = "2026-08-20"
lastmod = "2026-08-26"

og_title = "Converting an SVG to a PNG at the right size"
og_description = "Where the number of pixels actually comes from, and the three things that change when a drawing becomes pixels."
og_image_alt = "abox.tools - tools that never touch a server"
