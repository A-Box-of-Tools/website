/* Built from https://github.com/A-Box-of-Tools/website by build.py. Verify with: python build.py --check (names mangled by esbuild) */
import{trim as n}from"./specs.js";function m(e){return String(e??"").replace(/\.[^./\\]+$/,"")||"photo"}function f(e,t,o,r={}){const i=e.replace(/[^\w-]+/g,"-").replace(/^-+|-+$/g,"").slice(0,40)||"photo";if(o==="print"){const a=t.print?`${n(t.print.widthMm)}x${n(t.print.heightMm)}mm`:"print";return`${i}-${t.id}-${a}.jpg`}return o==="sheet"?`${i}-${t.id}-sheet-${r.paper??"print"}.jpg`:`${i}-${t.id}-${r.width}x${r.height}.jpg`}const s=e=>`${(e*100).toFixed(1)}%`;function l(e,t){const o=`${s(e.min)} to ${s(e.max)}`;return e.minMm!==void 0&&e.maxMm!==void 0?`${o} (${n(e.minMm)}-${n(e.maxMm)} mm)`:t?`${o} (${n(e.min*t)}-${n(e.max*t)} mm)`:o}function $(e,t,o){const r=e.mm!==null&&e.mm!==void 0?`${s(e.value)} (${n(e.mm)} mm)`:s(e.value),i=l(e,o);if(e.status==="ok")return`${t} is ${r}. The rule asks for ${i}.`;const a=e.status==="low"?t==="Eye line"?"too low in the frame":"too small":t==="Eye line"?"too high in the frame":"too large",h=t==="Eye line"?e.status==="low"?"Move the box down.":"Move the box up.":e.status==="low"?"Make the box smaller.":"Make the box larger.";return`${t} is ${r}, which is ${a}. The rule asks for ${i}. ${h}`}const g=(e,t=!1)=>e==="ok"?"good":t?"warn":"bad";function d(e){const t=Math.abs(e.degrees);if(t<.5)return"The eye line is level.";const o=e.degrees>0?"right":"left",r=e.status==="ok"?"which is within the couple of degrees an examiner will not notice.":"which is enough to be noticed. Retake it with the camera level, or straighten the picture first.";return`The head leans ${t.toFixed(1)} degrees to the ${o}, ${r}`}function p(e){const t=Math.abs(e.offset);if(e.status==="ok")return"The face is centred in the frame.";const o=e.offset>0?"right":"left";return`The face sits ${s(t)} of the frame's width to the ${o} of centre. Drag the box the other way, or press Fit again.`}function x(e){if(!e.enlarging)return`The crop is ${e.have.width} x ${e.have.height} pixels and the output is ${e.need.width} x ${e.need.height}, so nothing has to be invented.`;const t=`The crop is only ${e.have.width} x ${e.have.height} pixels and the output is ${e.need.width} x ${e.need.height}.`;return e.severe?`${t} That is a long way short: the result will look soft, and on paper it will look like a screenshot. A photograph taken closer, or at a higher resolution, is the only fix.`:`${t} It will be enlarged slightly, which costs a little sharpness and nothing else.`}function w(e,t){return e?t==="bad"?"The geometry meets the rule. The background does not, and that is the more common reason a photograph comes back.":"The geometry meets the rule and the background reads as acceptable.":"The geometry does not meet the rule yet. You can still save the files - nothing here refuses to give you your own photograph - but the figures above are what the form will be measuring."}export{l as bandText,p as centreText,f as outName,s as percent,w as readyText,x as resamplingText,g as statusClass,m as stemOf,d as tiltText,$ as verdictText};
