#
# 指南：比對兩個 JSON 檔案 - Traditional Chinese.
#

nav = "比對兩個 JSON 檔案"
title = "比對兩個 JSON 檔案 | 不帶格式化噪音"
description = "兩個檔案用同一種方式格式化，鍵排好序，再比對。為什麼生比 JSON 幾乎全是噪音，怎麼在瀏覽器裡把兩邊化成規範形，以及什麼能一路留到補丁裡。"

heading = "怎樣比對兩個 JSON 檔案"
lede = """兩個 JSON 檔案原樣拿來比對，亮起來的大半什麼都不是：縮排、折行、順序不同的鍵。藥方不是更聰明的比對——是先把兩個檔案過同一個格式化器，讓剩下的只有真差異。兩步都跑在你的瀏覽器裡，而裝著機密的配置檔案就該待在那裡。"""

updated = "2026年8月26日"

og_title = "不帶格式化噪音地比對兩個 JSON 檔案"
og_description = "把兩個檔案化成規範形：同樣的縮排、排好序的鍵，然後比對。之後亮起來的是真的，而且什麼都沒上傳。"
og_image_alt = "abox.tools - 從不碰伺服器的工具"
