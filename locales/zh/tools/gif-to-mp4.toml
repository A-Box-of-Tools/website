#
# GIF to MP4 - 简体中文。
#
# Overrides for tools/gif-to-mp4/tool.toml. Only words: the slug, the
# category, the icon, the CSP and the list of shared parts stay where they are.
#
# 改这一页的话术时，有两件事必须守住，跟英文原文一样。
#
# 第一件：视频一帧一帧地保留 GIF 的时间。GIF 没有帧率，只有每一帧各自的
# 延时，而且各不相同；一个把它重采样到固定帧率的转换器会把有些帧翻倍、
# 有些帧扔掉，幻灯片式的 GIF 就走样了。这里每一帧就是一个样本，时长就是
# 它的延时，核对的时候会把帧数数回来。
#
# 第二件：老实说清什么变了：GIF 自己会循环而视频不会（由播放器决定），
# GIF 可以让底下的东西透出来而视频不能（页面会问那里填什么颜色）。
# 两件事都写在页面上，而不是事后才发现。
#
# 用词：frame 写「帧」，delay 写「延时」，frame rate 写「帧率」，loop 写
# 「循环」，transparent 写「透明」，resample 写「重采样」。格式名一律不译。
#

name = "GIF 转 MP4"
heading = "GIF 转 MP4 <span class=\"h1-kw\">同一段动画，十分之一的体积</span>"
tagline = "每一帧都带着 GIF 给它的延时，变成 MP4 里的 H.264。在你的机器上转，文件从不上传。"

title = "GIF 转 MP4 | 免费，浏览器里完成，不上传 | abox.tools"
description = "在浏览器里把动态 GIF 变成 MP4，体积只剩零头。每一帧保留 GIF 自己的时间，不做重采样。MP4 里的 H.264，每个平台都收的文件。什么都不上传。"
og_title = "把 GIF 变成 MP4，不用上传"
og_description = "同一段动画变成 MP4 里的 H.264，通常只有十分之一大，每一帧的时间原样保留。在你的浏览器里转，文件从不离开你的机器。"
og_image_alt = "在浏览器里把 GIF 转成 MP4，不用上传"

pledge = """
    你选的那个 GIF，解码、一帧一帧画出来、编码、写进 MP4，全都在这台机器的内存里完成，\
    用的是你自己浏览器里的编码器，和从这个地址发来的代码。这里根本没有能做上传的东西，\
    这一页的另一头也没有服务器可以接收。"""

live_hint = """
      别听我们说。打开开发者工具，盯着网络面板，然后转点什么。没有任何一条\
      请求带着 GIF、一帧画面或者文件名。或者干脆断网，照样能转。"""

read_first = """
，GIF 的延时是怎么变成视频的时间的，看 <code>src/plan.js</code>；\
      画帧和编码看 <code>src/encode.js</code>；读取器看 \
      <code>src/shared/gif-decode.js</code>。这几个都够不着网络，旁边负责写的部分也一样。"""

howto_heading = "怎么把 GIF 转成 MP4"

card = """
            同一段动画变成 MP4，通常只有十分之一大，给那些不收大 GIF 的\
            地方。每一帧保留 GIF 给它的延时，不做重采样，画面尺寸不变。\
            在你的机器上编码。"""

[words]
plural = "GIF"
choose = "一个 GIF"
analytics_extra = """，里面\
  是什么，一样都不会"""

[[facts]]
text = "不上传"

[[facts]]
text = "不注册"

[[facts]]
text = "断网可用"

[[facts]]
text = "开源"

[[facts]]
text = "文件留在你的设备上"

[[privacy]]
title = "为什么 MP4 只有十分之一大，为什么每个平台都更想要它。"
body = """\

      GIF 把每一帧都当成一张图存起来，最多 256 种颜色，也不知道上一帧长什么样。\
      视频编码存的是变化的部分，全彩，而且 H.264 干这件事已经练了三十年；同一段\
      动画出来只有十分之一大，常常更小，看起来还更好。这就是为什么每一个社交平台、\
      聊天软件和内容系统要么拒绝大 GIF，要么在上传时悄悄把它变成 MP4，也是为什么\
      当什么地方说「GIF 太大」的时候，它想要的从来都是那个 MP4。"""

[[privacy]]
title = "上传才是最慢的那一步，而这一步在这里不会发生。"
body = """\

      每一个在线转换器都要先把文件拿到手：30 MB 沿着你的网络传上去，为的是换 3 MB \
      传回来，这还没算谁留着那份文件的问题。这一页用从这个地址发来的代码读 GIF，用\
      你浏览器里本来就有的编码器写 MP4，所以唯一在动的字节，是从你的硬盘到内存再\
      回去。这一页的 <code>Content-Security-Policy</code> 列出了它可以联系的每一个\
      地址，没有一个属于本站，而且断了网它照样能用。"""

[[privacy]]
title = "时间一帧一帧地保留，这一点大多数转换器都做错了。"
body = """\

      GIF 没有帧率。每一帧自己说要停多久，而且各不相同：幻灯片式的 GIF 一张图停两秒，\
      然后一口气翻十张。一个挑一个帧率再重采样的转换器会把有些帧翻倍、有些帧扔掉，\
      幻灯片出来就一顿一顿的或者变短了。这里 GIF 的每一帧就是视频的一帧，时长正好\
      是它的延时。唯一的一点自作主张，是每个浏览器都会做的那一点：不到两百分之一秒\
      的延时按十来播。做好的文件会重新打开，数一遍每一帧都在，而且播放时长跟 GIF \
      一样。"""

[[privacy]]
title = "视频做不到、GIF 做得到的两件事，事先说清。"
body = """\

      GIF 可以让底下的东西透出来；视频是一整块实心的矩形。GIF 透明的地方得填一种\
      颜色，页面会问填哪种，只在 GIF 确实有透明的地方时才问，默认白色，因为大多数\
      网页是白的。还有，GIF 自带循环的指令；MP4 没有，视频循环不循环由播放它的东西\
      决定。大多数信息流和聊天软件会循环播放短视频。这里的预览会循环，好让你看到\
      接缝。"""

[[privacy]]
title = "它在你机器上的代价，如实说。"
body = """\

      编码要花你的机器所需的时间：有硬件编码器的话（大多数笔记本和近十年的每一部\
      手机都有），普通的 GIF 几秒钟；没有的话更久。GIF 的每一帧要先解码到内存里，\
      上限就在这里：一个几兆的 GIF 展开后每帧每像素一个字节，页面读到半个 GB 的帧\
      就会停下来并说明，而不是把标签页搞崩。「取消」随时有效。"""

[[privacy]]
title = "Google 加载了什么，又没拿到什么。"
body = """\
 广告和\
      统计脚本来自 Google。它们都拿不到任何关于你 GIF 的东西：没有文件、没有\
      一帧画面、没有文件名、大小或时长。每一行读取、编码或写出它的代码都从这个源\
      发出，并列在代码仓库里。"""

[[privacy]]
title = "打赏按钮加载了什么，又没拿到什么。"
body = """\

      顶部的「Buy me a coffee」按钮由 cdnjs.buymeacoffee.com 的一个脚本画出来，\
      字体来自 Google Fonts。它只是一个链接，不多不少：它不上报访问，也拿不到\
      任何关于你或你 GIF 的东西。除非你点它，否则什么都不会发生，点了以后去的\
      是别人的网站。"""

[[privacy]]
title = "断网也能用。"
body = """\
 断开网络，这一页上的所有东西照常工作。这是最简单的证明：\
      一个把你的 GIF 发出去转换的工具会停下来。"""

[[howto]]
title = "选那个 GIF。"
body = """\
 一次一个。浏览器直接从你的硬盘读它，页面会说它是什么：多大、\
      多少帧、播多久、多少像素。"""

[[howto]]
title = "读那一行说明。"
body = """\
 一行：尺寸、帧数和它们的时间、时长、码率。没有什么要设的，\
      除非 GIF 有透明的地方，那时会出现一个颜色框，问那里填什么。"""

[[howto]]
title = "转换，然后读那句写着「已核对」的话。"
body = """\

      每一帧画出来再编码，进度条会说到哪了。然后做好的文件会在这里重新打开，\
      它必须跟 GIF 播得一样久，每一帧都在。它在下载按钮下面从内存里循环播放，\
      你能看到接缝。"""

[[faq]]
q = "为什么 MP4 小这么多？"
a = """
        因为 GIF 把每一帧都当成一张最多 256 色的图存起来，也不记得上一帧长什么样，\
        而视频编码只存变化的部分。H.264 干这件事已经练了三十年。同一段动画通常\
        出来只有十分之一大，常常更小，看起来还更好，因为它不再被限制在 256 种颜色，\
        也不用再靠抖动来掩盖这一点。很小的 GIF，或者几乎不动的 GIF，转成视频可能\
        反而变大；发生的时候页面会说。"""

[[faq]]
q = "时间会一样吗？"
a = """
        一样，一帧不差。GIF 没有帧率，只有每一帧各自的延时，视频把每一个延时原样\
        保留：GIF 的每一帧就是视频的一帧，停的时间一模一样。不重采样，不翻倍，\
        不丢帧。唯一的一点自作主张是每个浏览器都会做的那一点，不到两百分之一秒的\
        延时按十来播，所以视频的时长跟 GIF 在浏览器里播的一样。做好的文件会重新\
        打开核对这两点。"""

[[faq]]
q = "它会循环吗？"
a = """
        这由播放它的东西决定。GIF 自带循环的指令；MP4 没有这种东西，由播放器决定。\
        大多数信息流和聊天软件会循环播放短视频，这一页的预览也循环，好让你看到接缝。\
        桌面上的视频播放器通常只播一遍。"""

[[faq]]
q = "透明的地方怎么办？"
a = """
        会填一种颜色，因为视频是一整块实心的矩形。GIF 有透明的地方时，页面会显示\
        一个颜色框，默认白色，因为大多数网页是白的；没有透明的地方就不显示。颜色\
        在编码之前画在帧的底下，所以它是画面的一部分。"""

[[faq]]
q = "它会改画面尺寸吗？"
a = """
        最多改一个像素，而且只在不得不改的时候。H.264 要求两条边都是偶数，所以宽\
        或高是奇数的 GIF 会加一行背景色；不重采样。宽于 3840 像素的 GIF 会缩到这个\
        数，因为那是编码器靠得住的上限。页面在开始之前会说它要写的尺寸。"""

[[faq]]
q = "要多久？"
a = """
        有硬件编码器的机器上，普通的 GIF 几秒钟，大多数笔记本和近年的每一部手机\
        都有；没有的话更久。GIF 要先解码到内存里，很长的一个可能超过页面愿意装下\
        的量，它读到半个 GB 的帧就会停下来并说明。进度条会说到第几帧了，取消立刻\
        停下，什么都不写。"""

[[faq]]
q = "我的 GIF 会被上传到哪里吗？"
a = """
        不会。GIF 由你自己的浏览器在你自己的硬件上读取、解码、编码、写出，这一页的 \
        <code>Content-Security-Policy</code> 列出了它可以联系的每一个地址，没有一个\
        属于本站。最简单的自证办法是断开网络照样用。"""

[[faq]]
q = "手机上能用吗？"
a = """
        浏览器能编码视频的手机上能用，而且手机自带的硬件编码器很快。手机缺的是内存：\
        GIF 的帧要先解码到内存里，所以很长的一个可能超出手机能装下的量。"""

[[faq]]
q = "有大小限制吗？要钱吗？"
a = """
        页面读到半个 GB 的解码帧就会停下来，那已经是非常多的帧了，它会说明。它是\
        免费的，不注册、不登录，也没有试用期。本站带广告，那是养活它的东西；广告\
        拿不到任何关于你 GIF 的内容。"""

[[faq]]
q = "断网能用吗？"
a = """
        能。把页面加载一次，然后断开网络，它照样能用。这也是证明什么都没被上传的\
        最简单的办法：一个把你的 GIF 发出去转换的工具，你一拔网线它就停了。"""

[schema]
description = "在浏览器里把动态 GIF 转成 MP4。每一帧保留 GIF 自己的延时，画面尺寸不变，结果是 MP4 里的 H.264，体积只有 GIF 的零头。什么都不上传。"
features = [
  "读任何动态 GIF，处置规则和透明按浏览器的画法处理",
  "保留每一帧和每一个延时；不重采样到固定帧率",
  "只在 GIF 有透明的地方时，问透明处填什么颜色",
  "写出 MP4 里的 H.264，通常只有十分之一大",
  "重新打开做好的文件数帧、核对时长，并从内存里循环播放它",
  "断网可用；不上传、不注册",
]

[picker]
title = "把 GIF 拖到这里"
hint = "或者点击选择，一次一个动态 GIF"
