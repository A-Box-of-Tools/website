#
# 指南：比对两个 JSON 文件 - Simplified Chinese.
#

nav = "比对两个 JSON 文件"
title = "比对两个 JSON 文件 | 不带格式化噪音"
description = "两个文件用同一种方式格式化，键排好序，再比对。为什么生比 JSON 几乎全是噪音，怎么在浏览器里把两边化成规范形，以及什么能一路留到补丁里。"

heading = "怎样比对两个 JSON 文件"
lede = """两个 JSON 文件原样拿来比对，亮起来的大半什么都不是：缩进、折行、顺序不同的键。药方不是更聪明的比对——是先把两个文件过同一个格式化器，让剩下的只有真差异。两步都跑在你的浏览器里，而装着机密的配置文件就该待在那儿。"""

updated = "2026年8月26日"

og_title = "不带格式化噪音地比对两个 JSON 文件"
og_description = "把两个文件化成规范形：同样的缩进、排好序的键，然后比对。之后亮起来的是真的，而且什么都没上传。"
og_image_alt = "abox.tools - 从不碰服务器的工具"
