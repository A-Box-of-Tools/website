#
# Conversor de YAML a JSON - Spanish.
#
# Overrides for tools/yaml-to-json/tool.toml. Only words: the slug, the
# category, the icon, the CSP and the list of shared parts stay where they are.
#

name = "Conversor de YAML a JSON"
heading = "YAML&nbsp;a&nbsp;JSON <span class=\"h1-kw\">&mdash; y de JSON de vuelta a YAML</span>"
tagline = "Los dos sentidos, y te dice lo que cuesta cada uno. Nada de esto se pega en el servidor de otro."

title = "Conversor de YAML a JSON &mdash; en los dos sentidos, sin subir nada"
description = "Convierte YAML a JSON y JSON a YAML en tu navegador. Lee YAML 1.2, así que yes y no siguen siendo cadenas, y dice exactamente qué pierde cada sentido. No se sube nada: un archivo de configuración nunca sale de tu dispositivo."
og_title = "Convertir YAML sin pegarlo en un servidor"
og_description = "Un conversor de YAML y JSON que corre en tu propio dispositivo. Las claves mantienen su orden, los números sus dígitos, y lo que pegas no va a ninguna parte."
og_image_alt = "Conversor de YAML a JSON - los dos sentidos, sin subir nada"

pledge = """
    Convertir es aritmética sobre una cadena de texto, hecha aquí, en esta
    página. Los dos analizadores están escritos a mano y viven en
    <code>src/</code> &mdash; <code>yaml.js</code> y <code>json.js</code>
    &mdash; y no hay nada más. Esta herramienta no tiene función de red de
    ninguna clase &mdash; nada que descargar, nada que enviar &mdash; y aquí eso
    importa más que casi en ningún otro sitio de esta web: un archivo YAML suele
    ser una configuración de despliegue, y una configuración de despliegue suele
    estar llena de nombres de host, nombres de bucket y secretos."""

live_hint = """
      No hace falta que nos creas: abre las DevTools, mira la pestaña de red y
      convierte algo. Ni una sola petición lleva un carácter de lo que pegaste. O
      desconéctate de internet y conviértelo igualmente."""

read_first = """
, <code>src/yaml.js</code> para el lector que
      rechaza un ancla en lugar de adivinar qué quería decir, y
      <code>src/convert.js</code> para saber por qué una conversión es un
      analizador y un impresor sin nada en medio que conozca los dos formatos a
      la vez."""

howto_heading = "Cómo convertir YAML a JSON sin subirlo"

card = """
            Convierte YAML en JSON o JSON en YAML, y que te digan lo que cuesta
            el viaje. Pega una configuración de despliegue sin entregársela a
            nadie."""

[words]
plural = "YAML y JSON"
choose = "un archivo"
analytics_extra = """, ni del YAML ni del
  JSON que pasa por aquí"""

[[facts]]
text = "Sin subidas"

[[facts]]
text = "Sin cuenta"

[[facts]]
text = "Sin límite de tamaño"

[[facts]]
text = "Funciona sin conexión"

[[facts]]
text = "Código abierto"

[[privacy]]
title = "Lo que pegas no tiene adónde ir."
body = """
 La
      Content-Security-Policy nombra todas las direcciones que esta página puede
      contactar, y ninguna de ellas es nuestra. Aquí no hay ningún punto final
      donde recoger una configuración pegada, ni nada en el código que la
      enviaría si lo hubiera."""

[[privacy]]
title = "Aquí nada descarga nada."
body = """
 No hay ningún
      <code>fetch</code>, ningún <code>XMLHttpRequest</code> ni ningún
      <code>sendBeacon</code> en todo <code>src/</code>. Los dos analizadores y
      los dos impresores son funciones de esta página que toman una cadena y
      devuelven una cadena."""

[[privacy]]
title = "Lee YAML 1.2, así que Noruega sigue siendo Noruega."
body = """
 En YAML 1.1,
      <code>no</code> era un booleano, que es el error famoso que convierte el
      código de país de Noruega en <code>false</code>. Esto lee 1.2, donde es la
      cadena que parece. En el otro sentido esas palabras se escriben de vuelta
      <em>entre comillas</em>, porque lo que abra el archivo después puede seguir
      siendo un lector de 1.1. <code>tests/js/text-convert.test.js</code>
      comprueba las dos mitades."""

[[privacy]]
title = "Una conversión que no puede ser honesta se detiene."
body = """
 Un ancla, un
      alias o una etiqueta en el YAML terminan la conversión con un mensaje que
      dice en qué línea está, en lugar de un documento JSON que en silencio
      significa otra cosa. JSON no tiene manera de decir &laquo;el mismo nodo dos
      veces&raquo;, y elegir una interpretación sería decidir por ti."""

[[privacy]]
title = "Qué carga Google, y qué no llega a ver."
body = """
 Los scripts de
      publicidad y de medición vienen de Google, y el botón de donación de Buy Me
      a Coffee. A ninguno se le entrega un carácter de tu texto. Cada línea que
      lo lee, lo analiza o lo escribe se sirve desde este origen y está en el
      repositorio."""

[[privacy]]
title = "Funciona sin conexión."
body = """
 Desconecta la red y la herramienta sigue
      igual, porque nunca hubo un paso de red en ella. Es la prueba más simple de
      todas."""

[[howto]]
title = "Elige el sentido."
body = """
 <em>YAML a JSON</em> o <em>JSON a
      YAML</em>. La nota bajo el menú dice lo que pierde ese sentido antes de que
      pegues nada, y no después."""

[[howto]]
title = "Pégalo, o suelta el archivo."
body = """
 Vale cualquier cosa que puedas
      seleccionar y copiar. Un archivo soltado sobre el selector lo lee tu propio
      navegador y lo pone en la caja &mdash; no hay paso de subida que omitir
      &mdash; y una extensión <code>.json</code> o <code>.yaml</code> te elige el
      sentido."""

[[howto]]
title = "Elige la indentación."
body = """
 Dos espacios, cuatro, o un tabulador.
      El tabulador se ofrece solo para JSON: YAML está definido en términos de
      espacios, y un tabulador no es indentación válida en él."""

[[howto]]
title = "Lee el error donde está el error."
body = """
 Un analizador que falla aquí
      dice qué encontró y en qué línea y columna, en lugar de &laquo;token
      inesperado en la posición 4193&raquo;. Eso suele bastar para arreglar un
      archivo de configuración sin abrir nada más."""

[[howto]]
title = "Llévate el resultado."
body = """
 Cópialo, o descárgalo como archivo, con el
      nombre del formato en el que salió."""

[[faq]]
q = "¿Se sube mi YAML a alguna parte?"
a = """
        No. Los dos analizadores y los dos impresores de esta página son
        funciones que corren en tu propio navegador, en tu propio hardware. Esta
        herramienta no tiene función de red de ninguna clase &mdash; nunca
        descarga nada y nunca envía nada &mdash; y la
        <code>Content-Security-Policy</code> de la página nombra todas las
        direcciones que puede contactar, ninguna de las cuales es nuestra. Esa es
        la razón para usarlo con una configuración de despliegue: están llenas de
        nombres de host, nombres de bucket y de vez en cuando algún secreto que
        alguien pensaba mover, y pegar una en el conversor de otro es
        entregársela."""

[[faq]]
q = "¿Qué se pierde al convertir YAML a JSON?"
a = """
        Los comentarios, porque JSON no tiene dónde ponerlos. Las anclas, los
        alias y las etiquetas se rechazan directamente en lugar de adivinarse
        &mdash; cada una dice algo que JSON no puede decir, y un conversor que en
        silencio eligiera una interpretación te entregaría un documento que no es
        lo que decía el archivo. El otro sentido no pierde nada: todo documento
        JSON ya es un documento YAML."""

[[faq]]
q = "Mi YAML dice no y el JSON salió como cadena. ¿Por qué?"
a = """
        Porque es una cadena, y esto lee YAML 1.2 en lugar de 1.1. En YAML 1.1,
        <code>yes</code>, <code>no</code>, <code>on</code> y <code>off</code> eran
        booleanos, que es el error famoso que convierte el código de país de
        Noruega en <code>false</code>. YAML 1.2 lo dejó caer y esto también: solo
        <code>true</code>, <code>false</code>, <code>null</code> y <code>~</code>
        se leen como algo que no sea texto. En el otro sentido, esas palabras se
        escriben de vuelta <em>entre comillas</em>, aunque esto las leería como
        texto sin ellas, porque lo que abra el archivo después quizá no. PyYAML
        sigue usando 1.1 por defecto. Leer estrictamente y escribir de forma
        conservadora es la única combinación que acierta en los dos sentidos."""

[[faq]]
q = "¿Mantiene el orden de mis claves?"
a = """
        Sí, en los dos sentidos, y eso es más difícil de lo que parece. Un
        conversor construido sobre <code>JSON.parse</code> mueve en silencio al
        principio las claves que parecen enteros, así que
        <code>{"10":a,"2":b}</code> vuelve como <code>{"2":b,"10":a}</code>. Los
        números mantienen los dígitos que escribiste, así que un identificador de
        cuenta de veinte dígitos no pierde los tres últimos por un double. Si
        <em>quieres</em> que se ordenen hay una casilla, y ordena por cómo se leen
        las claves y no por sus puntos de código."""

[[faq]]
q = "¿Puede convertir varios documentos YAML a la vez?"
a = """
        No, y lo dice en lugar de elegir uno. Un archivo con separadores
        <code>---</code> contiene más de un documento, y JSON no tiene ninguna
        forma que signifique &laquo;varios documentos&raquo;: un array sería una
        afirmación que el archivo nunca hizo. Conviértelos de uno en uno."""

[[faq]]
q = "¿Por qué aquí no hay un formateador de YAML?"
a = """
        Porque YAML no tiene una forma comprimida que valga la pena escribir: la
        corta es el estilo de flujo, que es ilegible, y lo ilegible es lo
        contrario de la razón por la que se mantiene un archivo en YAML. Ordenar
        JSON, XML, HTML y CSS es el trabajo del
        <a href="../formatear-json/">formateador de JSON</a>, y ese también
        ordena YAML."""

[[faq]]
q = "¿De qué tamaño puede ser el archivo?"
a = """
        Aquí no hay ningún límite puesto, porque no hay un servidor pagándolo. El
        techo real es tu propio dispositivo: unos cuantos megabytes de YAML van
        bien, y con un documento muy largo la página espera a que hagas una pausa
        al escribir antes de convertirlo, en lugar de pelearte el teclado."""

[[faq]]
q = "¿Es gratis? ¿Necesito una cuenta?"
a = """
        Es gratis, y no hay cuenta, ni inicio de sesión, ni versión de prueba, ni
        límite de cuánto pegas. El sitio lleva publicidad, que es lo que lo paga;
        a los anuncios no se les da nada sobre tu texto."""

[[faq]]
q = "¿Funciona sin conexión?"
a = """
        Sí. Carga la página una vez, desconéctate de internet y sigue
        funcionando. Esa es también la forma más simple de demostrar que no se
        sube nada: una herramienta que mandara tu configuración fuera para
        convertirla se pararía en el momento en que te desconectaras."""

[schema]
description = "Convierte YAML a JSON y JSON a YAML en el navegador. Lee YAML 1.2, mantiene el orden de las claves y los dígitos exactos de cada número, y dice qué pierde cada sentido. No se sube nada."
features = [
  "Convierte YAML a JSON y JSON a YAML",
  "Lee YAML 1.2, así que yes y no siguen siendo cadenas",
  "Mantiene el orden de las claves y los dígitos exactos de cada número",
  "Rechaza anclas, alias y etiquetas en lugar de adivinar qué querían decir",
  "Dice en qué línea y columna está el error, con palabras",
  "Funciona con un archivo soltado igual que con un pegado",
  "Funciona sin conexión; sin subidas, sin cuenta",
]

[picker]
title = "Suelta aquí un archivo"
hint = "o haz clic para buscarlo &mdash; un archivo .yaml, .yml o .json"
