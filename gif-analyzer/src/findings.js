/* Built from https://github.com/A-Box-of-Tools/website by build.py. Verify with: python build.py --check (names mangled by esbuild) */
import{isFullCanvas as $,duration as k}from"./frames.js";const y={bad:0,warn:1,note:2};function I(t,s={}){const m=[],a=(e,o,i)=>m.push({level:e,title:o,body:i}),n=t.frames,r=(e,o,i)=>`${e.toLocaleString()} ${e===1?o:i}`;n.length===0&&a("bad","There are no frames in this file.","The header parsed, but no image block followed it. Whatever this is, no viewer will draw anything from it.");for(const e of t.problems)a("bad","The file does not end cleanly.",e);if(t.trailingBytes>0&&a("warn",`${h(t.trailingBytes)} sit after the end of the file.`,"Every decoder stops at the trailer byte, so these are never read and never drawn. They are usually a second file appended to the first, or an editor that wrote past itself. Cutting the file at the trailer loses nothing."),n.length>1){const{nominal:e,real:o,clamped:i}=k(n);i>0&&a("warn",`${r(i,"frame asks","frames ask")} to be held for less than 0.02 seconds, and no browser will do it.`,`Every browser since Netscape rounds a delay under two hundredths of a second up to ten - a rule written in 1996 for the spinning globes of the time and never removed. So this animation says it runs for ${f(e)} and actually plays for ${f(o)}, about ${(o/Math.max(e,1)).toFixed(1)} times slower. Writing 0.02 instead of 0.01 gets the speed it was asking for.`);const d=n.filter(l=>l.delay===0).length;d>0&&d===n.length&&a("warn","Every frame has a delay of zero.","The file gives no timing at all, so what it plays at is entirely up to the viewer - usually ten frames a second, but nothing guarantees it. Two different browsers can play this at two different speeds and both be right.");const c=new Set(n.map(l=>l.delay));if(c.size>1){const l=[...n].sort((b,v)=>b.delay-v.delay);a("note",`The frames are not evenly timed: ${c.size} different delays, from ${f(l[0].delay)} to ${f(l[l.length-1].delay)}.`,"A held final frame is deliberate and common. A scatter of delays through the middle is usually a converter dropping frames from a video and stretching the neighbours to cover the gap.")}}if(n.length>1&&(t.loop===null?a("warn","There is no loop block, so this plays once and stops.","Looping is not part of the GIF specification. It is a block Netscape invented in 1995 that everything implemented anyway, and a file without one is a file every browser shows exactly once. If this was meant to loop, whatever wrote it left the block out."):t.loop>0&&a("note",`This plays ${r(t.loop,"time","times")} and then stops.`,"The loop block names a count rather than the usual zero, which means forever. Some older viewers ignore the count and loop anyway.")),n.length>1){const e=n.filter(i=>$(t,i)).length;e===n.length?a("warn","Every frame redraws the whole canvas.",`A GIF may store a frame as only the rectangle that changed since the last one, which is why a screen recording of a mostly-still window can be small. Nothing here does: every frame is the full ${t.width}&times;${t.height}. That is correct for frames made from unrelated pictures, and is money left on the table for anything filmed or recorded, where most of the picture does not move between frames.`):e>0?a("note",`${r(e,"frame","frames")} of ${n.length} redraw${e===1?"s":""} the whole canvas; the rest store only the part that changed.`,"That is what an optimised GIF looks like. The full-canvas ones are usually the first frame and any point where the picture changed completely."):a("note","No frame redraws the whole canvas.","Every frame after the first stores only the rectangle that changed, which is the format used properly and is why this file is as small as it is.");const o=n.filter(i=>i.left+i.width>t.width||i.top+i.height>t.height);o.length>0&&a("bad",`${r(o.length,"frame reaches","frames reach")} outside the canvas.`,`The canvas is ${t.width}&times;${t.height} and ${o.length===1?"a frame":"some frames"} claim a rectangle that runs off the edge of it. Decoders disagree about what to do with the overhang: some clip it, some refuse the file.`)}const w=n.filter(e=>e.palette);if(w.length>0){const e=w.reduce((i,d)=>i+d.palette.bytes,0),o=e>t.size*.1?"warn":"note";a(o,`${r(w.length,"frame carries","frames carry")} a colour table of its own, costing ${h(e)} - ${g(e,t.size)} of the file.`,"A palette of its own lets a frame use colours the shared table does not have, and costs three bytes per entry every time it is written. Where the frames are of the same scene, one table for all of them is both smaller and steadier to watch: a palette that lurches between frames is what GIF flicker actually is.")}if(!t.globalPalette&&n.length>0&&a("note","There is no global colour table.","Nothing is wrong with that - every frame here brings its own - but it does mean a viewer has nothing to paint the canvas with before the first frame arrives."),s.waste&&s.waste.wastedEntries>0){const{wastedEntries:e,wastedBytes:o,declared:i,referenced:d}=s.waste,c=o>t.size*.05?"warn":"note";a(c,`${e.toLocaleString()} colour table entries are never used, which is ${h(o)}.`,`The tables in this file declare ${i.toLocaleString()} colours and the pixels refer to ${d.toLocaleString()} of them. A colour table is a power of two entries long whatever is in it, so a frame using nine colours still gets sixteen; some rounding up is unavoidable. Much more than that means the palettes were built for a different picture than the one that ended up in the frame.`)}s.colors!==void 0&&n.length>0&&a("note",`The whole animation draws with ${r(s.colors,"colour","colours")}.`,s.colors<=64?"That is flat artwork, and it is what GIF was designed for. A file like this compresses well and dithers badly, so if it looks speckled the dithering is the thing to turn off.":"A photograph, or something rendered from one. GIF gives a frame at most 256 colours, so anything with a gradient in it is being approximated - which is where dithering, and the noise that makes a file large, comes from.");for(const e of t.extensions)if(e.kind==="comment")a("note",`There is a comment block of ${h(e.dataBytes)} in this file.`,"Comments are not shown by any viewer and are carried by every copy of the file. They are usually the name of whatever wrote it, and occasionally something the author would not have chosen to publish.");else if(e.name.startsWith("XMP")){const o=e.bytes>t.size*.05?"warn":"note";a(o,`An XMP metadata packet takes up ${h(e.bytes)} - ${g(e.bytes,t.size)} of the file.`,"XMP is the XML block an image editor writes to record what it did, and it can carry the edit history, the software, and sometimes the author. No viewer draws any of it. Removing it costs the picture nothing.")}else e.name.startsWith("ICCRGBG1")?a("note",`An ICC colour profile takes up ${h(e.bytes)}.`,"The profile says what the numbers in the palette mean as colours. Almost nothing reads one out of a GIF, so it is usually weight for nothing - but dropping it can shift the colours in the viewers that do."):e.kind==="plain-text"?a("note","There is a plain-text extension in this file.","A block that asks the viewer to draw text over the picture in a grid of cells. It was in the 1989 specification, it was never implemented by anything, and every decoder skips it. Whatever it says will not appear."):e.kind==="application"&&e.loop===void 0&&a("note",`There is an application block named &ldquo;${T(e.name)}&rdquo;, of ${h(e.bytes)}.`,"Application blocks are how anything outside the specification gets into a GIF. Viewers skip the ones they do not recognise, so this one is weight unless something specific is reading it.");const u=n.filter(e=>e.interlaced).length;u>0&&a("note",`${r(u,"frame is","frames are")} interlaced.`,"The rows are stored in four passes so that a picture arriving down a slow line showed a coarse version of itself early. It made sense in 1993, it compresses slightly worse than storing the rows in order, and no viewer benefits from it now."),t.version!=="89a"&&a("note",`This file says it is GIF${t.version} rather than GIF89a.`,"GIF87a came first and has no timing blocks, no transparency and no loop block. A file claiming 87a while carrying any of those is common - decoders do not check - but it is a sign the header was copied rather than written."),t.globalPalette&&t.backgroundIndex>=t.globalPalette.count&&a("warn",`The background colour is index ${t.backgroundIndex}, and the global table only has ${t.globalPalette.count} entries.`,"Nothing will crash - viewers use transparent instead - but it means the field was never set, or was set for a palette that is no longer the one in the file."),t.aspect!==null&&a("note",`The pixel aspect ratio is set to ${t.aspect.toFixed(2)}.`,"A field for displays that did not have square pixels. Every modern viewer ignores it and draws the frame at its stored size, so a file relying on it will look stretched to whoever made it and normal to everybody else.");const p=s.decoded?s.decoded.reduce((e,o)=>e+(o?o.clears:0),0):0;if(p>n.length&&a("note",`The compressor reset its dictionary ${p.toLocaleString()} times.`,"LZW builds a dictionary as it goes and can hold 4,096 entries; when it fills, the encoder starts again. Once per frame is normal - it is how a frame begins. More than that means the picture was varied enough to exhaust the dictionary partway through, which is what a photograph or heavy dithering does, and is why those files are large."),s.identical>0&&a("warn",`${r(s.identical,"frame is","frames are")} identical to the frame before.`,"They cost their full compressed size and change nothing on screen. Adding their delay to the previous frame instead would look exactly the same and would be smaller."),s.decoded){const e=s.decoded.filter(o=>o&&(o.corrupt||o.truncated));e.length>0&&a("bad",`${r(e.length,"frame does","frames do")} not decode completely.`,`${e[0].corrupt??"The compressed data ends before the frame is full"}. A viewer will draw what it can and leave the rest of the rectangle blank.`)}return m.sort((e,o)=>y[e.level]-y[o.level]),m}const h=t=>t<1024?`${t} bytes`:t<1024*1024?`${(t/1024).toFixed(t<10240?1:0)} KB`:`${(t/1048576).toFixed(1)} MB`,g=(t,s)=>s>0?`${Math.round(t/s*100)}%`:"0%",f=t=>{const s=t/100;return s>=10?`${s.toFixed(1)}s`:`${s.toFixed(2)}s`},T=t=>t.replace(/[&<>"']/g,s=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[s]);export{I as findings};
