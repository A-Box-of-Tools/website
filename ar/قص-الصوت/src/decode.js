/* Built from https://github.com/A-Box-of-Tools/website by build.py. Verify with: python build.py --check (names mangled by esbuild) */
import{sniffSampleRate as l}from"./samplerate.js";const d=48e3;class a extends Error{constructor(t){super(t),this.name="UnreadableFile"}}async function h(r){const t=new Uint8Array(await r.arrayBuffer());if(!t.length)throw new a("That file is empty.");const i=l(t),s=i??d;let n;try{n=await new OfflineAudioContext(1,1,s).decodeAudioData(t.slice().buffer)}catch(e){throw new a("This browser could not read any sound out of that file. Either the format is one it does not decode, or the file has no audio track in it at all. MP3, WAV, FLAC, M4A, MP4, MOV, WebM and Ogg all work; AVI, WMA and most MKVs do not.",{cause:e})}const o=[];for(let e=0;e<n.numberOfChannels;e+=1)o.push(n.getChannelData(e));if(!o.length||!o[0].length)throw new a("There is no sound in that file - it decoded to nothing.");return{channels:o,sampleRate:n.sampleRate,frames:o[0].length,duration:n.duration,guessedRate:i===null}}export{a as UnreadableFile,h as decodeAudio};
